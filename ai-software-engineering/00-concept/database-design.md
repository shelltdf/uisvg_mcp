# 数据库与存储设计

## 范围说明

本工具为纯前端 SPA，**无服务端数据库**。持久化以**文件**为主。

## 文件存储

| 类型 | 说明 |
|------|------|
| 设计稿 | `*.ui.svg`，格式见 [uisvg-format-spec.md](./uisvg-format-spec.md) |
| 用户选择 | 浏览器 `localStorage` 可存放最近文件句柄、主题偏好（可选，后续） |

## 约束

- 不在此阶段引入远程数据库；若未来同步云端，需新增 API 设计并归入 `interface-design*.md`。
